<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="nav-container">
            <div class="nav-logo"><span class="taxmate-animated">Tax Mate</span></div>
            <div class="nav-links">
                <a href="/dashboard">Dashboard</a>
                <a href="/filing/start">File Taxes</a>
                <a href="/auth/logout">Logout</a>
            </div>
        </div>
    </nav>

    <div class="form-container">
        <h1>📅 Tax Calendar</h1>
        <p style="color: #6b7280; margin-bottom: 2rem;">Never miss important tax deadlines</p>

        <div style="background: white; border-radius: 1rem; padding: 2rem; box-shadow: 0 4px 6px rgba(0,0,0,0.1);">
            <% deadlines.forEach(deadline => { %>
            <div style="display: flex; justify-content: between; align-items: center; padding: 1rem; border-bottom: 1px solid #e5e7eb;">
                <div>
                    <div style="font-weight: 600;"><%= deadline.title %></div>
                    <div style="color: #6b7280;"><%= new Date(deadline.date).toLocaleDateString() %></div>
                </div>
                <div style="color: "<%= deadline.passed ? '#ef4444' : '#10b981' %>;">
                    <%= deadline.passed ? 'Passed' : 'Upcoming' %>
                </div>
            </div>
            <% }); %>
        </div>

        <div style="text-align: center; margin-top: 2rem;">
            <a href="/dashboard" class="btn btn-primary">Back to Dashboard</a>
        </div>
    </div>
</body>
</html>